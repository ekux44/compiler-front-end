program example(input, output);
var x: integer ;
var y: integer ;
var z: real ;
procedure gcd(a : integer ; b: real );
begin
	if (b>0.0) or (z) then x:=2 mod a
	else f:= b/2
end ;
begin
	x := 5;
	call gcd(3.0, x)
end .