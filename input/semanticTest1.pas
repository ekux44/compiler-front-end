program example(input, output);
var x: integer ;
var y: integer ;
var z: real ;
procedure gcd(a : integer ; b: real );
begin
	if (b>0.0) or (x<>y) then x:=2 mod a
	else z:= b/2.0
end ;
begin
	x := 5;
	call gcd(x, y)
end .